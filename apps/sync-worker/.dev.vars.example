BITKUB_API_KEY=
BITKUB_API_SECRET=
# For multiple Bitkub accounts, use this secret map instead of the two legacy
# secrets above. Each key must exactly match the local accounts.id value.
BITKUB_ACCOUNTS_JSON=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
INTERNAL_PUSH_TEST_TOKEN=
