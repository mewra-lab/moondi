{
  "name": "@moondi/sync-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@mmmike/web-push": "^1.3.0",
    "@moondi/exchanges": "*",
    "@moondi/shared": "*"
  },
  "devDependencies": {
    "wrangler": "^4.63.0"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "dev": "wrangler dev --test-scheduled",
    "test": "vitest run",
    "types": "wrangler types",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run"
  }
}
